[build]
  base = "frontend"
  command = "npm install && npm run build && ls -la out/"
  # Next.js static export outputs to 'out' directory (relative to base)
  publish = "out"

[build.processing]
  skip_processing = false

# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
